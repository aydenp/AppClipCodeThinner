const removeAppClipAttrs = require("./remove-app-clip-attrs");
const renameDataColor = require("./rename-data-color");
const migrateSegmentStyles = require("./migrate-segment-styles");

module.exports = {
    removeAppClipAttrs, renameDataColor, migrateSegmentStyles
};